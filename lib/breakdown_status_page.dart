import 'package:flutter/material.dart';

class BreakdownStatusPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Breakdown Alerts Coming Soon!')),
    );
  }
}