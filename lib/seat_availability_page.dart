import 'package:flutter/material.dart';

class SeatAvailabilityPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Seat Availability Feature Coming Soon!')),
    );
  }
}